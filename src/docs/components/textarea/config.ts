import { textareaMetadata } from './metadata'

export const textareaConfig = {
  showInstallation: true,
  centerPreview: true,
  previewBackground: 'default',
  previewPadding: 'lg',

  installCommands: textareaMetadata.cli,

  demoMap: {
    basic: 0,
  },
}
