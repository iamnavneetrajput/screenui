import { inputMetadata } from './metadata'

export const inputConfig = {
  showInstallation: true,
  centerPreview: true,
  previewBackground: 'default',
  previewPadding: 'lg',

  installCommands: inputMetadata.cli,

  demoMap: {
    basic: 0,
  },
}
